import { defineConfig } from "windicss/helpers";

export default defineConfig({
  theme: {
    colors: {
      divider: "#a2a9b1",
      primary: "#22bbff",
      disabled: "#9d9d9d",
    },
  },
});
