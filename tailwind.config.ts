import { defineConfig } from "windicss/helpers";

export default defineConfig({
  theme: {
    colors: {
      divider: "#a2a9b1",
      "primary-light": "#22bbff",
      disabled: "#9d9d9d",
      paper: "#f8f8f8",
      "primary-main": "#6a6aff",
    },
  },
});
